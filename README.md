# Authentication
An authentication system verifies a user’s identity before granting access to an application or service using credentials like passwords, OTPs, biometrics, or tokens to ensure security.
